import React from 'react';

type HeaderProps = {
  // define props here
};

export const Header = ({}: HeaderProps) => {
  return (
    <div>
      <h1>Header component</h1>
    </div>
  );
};
